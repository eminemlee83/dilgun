/* 딜군 설정 — 여기에만 키를 넣습니다.
   화면 파일(viewer.html 등)을 새로 받아 덮어써도 이 파일은 그대로 두세요.
   그러면 키를 다시 넣을 일이 없습니다. */

window.DILGUN = {
  SUPABASE_URL: "https://YOUR-PROJECT-ID.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
